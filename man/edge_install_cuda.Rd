% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{edge_install_cuda}
\alias{edge_install_cuda}
\title{Install the CUDA backend for GPU-accelerated inference}
\usage{
edge_install_cuda(cuda_version = "13.1", force = FALSE, llama_build = "b8179")
}
\arguments{
\item{cuda_version}{CUDA version string. One of \code{"12.4"} or \code{"13.1"} (default).
Use \code{"13.1"} for Blackwell GPUs (RTX 50 series, sm_120).}

\item{force}{Re-download even if already installed.}

\item{llama_build}{llama.cpp build number to pull the backend from
(default: \code{"b8179"}, the version bundled in this package).}
}
\value{
Invisibly returns the path to the installed CUDA backend DLL.
}
\description{
Downloads a pre-built GGML CUDA backend shared library and stores it in the
edgemodelr cache. After installation, restart your R session (or call
\code{edge_reload_cuda()}) so the GPU backend is picked up during model loading.

On Windows, the CUDA runtime libraries (\code{cudart}, \code{cublas}) must also be
present. Run \code{\link{edge_install_cuda_toolkit}()} once after this function
to install them automatically.
}
\examples{
\dontrun{
edge_install_cuda()                  # GPU backend installed
edge_install_cuda_toolkit()          # Install CUDA runtime DLLs (Windows)
ctx <- edge_load_model("model.gguf", n_gpu_layers = 35)
}
}
\seealso{
\code{\link{edge_install_cuda_toolkit}}, \code{\link{edge_reload_cuda}},
\code{\link{edge_cuda_info}}
}
