% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{edge_cuda_info}
\alias{edge_cuda_info}
\title{Check whether a CUDA backend is installed and active}
\usage{
edge_cuda_info()
}
\value{
A named list with three elements:
\describe{
  \item{installed}{Logical. \code{TRUE} if a CUDA backend DLL is present in the
    edgemodelr cache directory.}
  \item{active}{Logical. \code{TRUE} if the CUDA backend has been loaded into
    the current R session (i.e. \code{edge_reload_cuda()} was called).}
  \item{path}{Character. Path to the active CUDA DLL, the installed DLL (if
    not yet active), or \code{NA} if no backend is found.}
}
}
\description{
Reports the installation and activation status of the GPU (CUDA) backend.
Use this to verify that GPU inference is available before loading a model
with \code{n_gpu_layers > 0}.
}
\examples{
\dontrun{
info <- edge_cuda_info()
if (info$active) {
  cat("GPU inference active:", info$path, "\n")
} else if (info$installed) {
  cat("CUDA backend installed but not active. Call edge_reload_cuda().\n")
} else {
  cat("No CUDA backend. Run edge_install_cuda() to enable GPU support.\n")
}
}
}
\seealso{
\code{\link{edge_install_cuda}}, \code{\link{edge_install_cuda_toolkit}},
\code{\link{edge_reload_cuda}}
}
